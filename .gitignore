node_modules/
dist/
*.db
*.db-wal
*.db-shm
# Copies the service writes next to its database (src/services/backup.ts)
backups/
*.db.tmp-*
.env

# Local dev overrides (publishes mesh port to host, etc.)
docker-compose.override.yml

# CLI binaries
cli/moshi
cli/moshi-*

# Design docs (kept in plexus)
docs/
build_onepager.py

# Docker Go-build artifacts (binaries baked into image, never committed)
cli-dist/

# OS + editor cruft
.DS_Store
.claude/

# SQLite runs in WAL mode; the sidecars hold the most recent pages (payloads,
# token hashes). `*.db` alone does not cover them, and this repo is public.
*.db-wal
*.db-shm

# Written into the repo root by the Playwright MCP server.
.playwright-mcp/
